# Session: 2026-08-10 22:58–23:12 UTC

## Context
User returned to econ-sim refactor after previous session ended with incomplete work. Goal was to fix the `getOutgoingEdges is not a function` error and complete the graph-tools integration.

## What Happened

### Initial State
- `src/graph.js` already contained the adapter wrapping `@space-cadet/graph-core@0.1.3`
- `src/ui.js`, `src/simulation.js`, `src/scenarios.js` all imported from `./graph.js?v=7`
- Browser showed `TypeError: this._graph.getOutgoingEdges is not a function`

### Attempt 1: Switch CDN (jsdelivr)
- Changed import from `esm.sh` to `cdn.jsdelivr.net/npm/.../dist/index.mjs`
- Bumped versions `?v=7` → `?v=8`, `?v=10` → `?v=11`
- **Result**: Bare specifier error — `import "mathjs"` in raw npm file, browser can't resolve
- Lesson: jsdelivr serves raw npm files, not browser-ready bundles

### Attempt 2: esm.sh direct path
- Changed import to `esm.sh/@space-cadet/graph-core@0.1.3/es2022/graph-core.mjs`
- Bumped versions again `?v=8` → `?v=9`, `?v=11` → `?v=12`
- **Result**: Still broken — esm.sh build appears incomplete for this freshly published package
- Lesson: esm.sh builds lazily; freshly published packages may not have complete builds available

### Final Decision
User: "Not working. We'll fix it later." — Session paused.

## Files Changed (workspace + deployed)
- `src/graph.js` — CDN import path changed 3 times
- `src/ui.js` — `?v=7` → `?v=12`
- `src/simulation.js` — `?v=7` → `?v=9`
- `src/scenarios.js` — `?v=7` → `?v=9`
- `index.html` — `?v=10` → `?v=12`

## Current State (PAUSED)
- `graph.js?v=9` imports from `esm.sh/@space-cadet/graph-core@0.1.3/es2022/graph-core.mjs`
- App is BROKEN — graph does not render, simulation fails
- Need to find reliable way to load graph-core in browser

## Options for Next Session
1. Build self-contained IIFE bundle of graph-core and vendor it (no CDN)
2. Republish as v0.1.4 and wait for esm.sh cache to settle
3. Use a different bundling approach (e.g., skypack, unpkg with ?module)
4. Revert to `graph-legacy.js` temporarily and retry refactor later

## Learnings
- esm.sh cache is `immutable` (1 year) — any bad response gets locked in
- jsdelivr raw npm files contain bare specifiers — unusable in browser directly
- esm.sh builds packages lazily on first request — freshly published packages are risky
- For freshly published packages, better to vendor a self-built bundle than rely on CDN
